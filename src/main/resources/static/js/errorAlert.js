function errorAlertUsernameDupl(){
    // if(message && (typeof message === "string" || message instanceof String)){
    //     let getParam = window.location.search.substring(1);
        alert("Given username is in use. Please try again.")
}